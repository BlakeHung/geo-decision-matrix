services:
  geo-ai:
    build: .
    container_name: geo_decision_matrix
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: tail -f /dev/null